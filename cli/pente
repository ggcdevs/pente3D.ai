#!/usr/bin/env bash
# Convenience launcher: `cli/pente <verb> …` runs the CLI via tsx (no build step).
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
root="$(cd "${here}/.." && pwd)"
exec node "${root}/node_modules/.bin/tsx" "${here}/main.ts" "$@"
