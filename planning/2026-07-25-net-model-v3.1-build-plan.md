# Networked Game Model v3.1 — build plan (epic #47)

- **Date:** 2026-07-25
- **Status:** Design agreed with the user (`planning/2026-07-24-net-model-v3.1-design.md`); the #45
  repro is landed and failing. Ready to build.
- **Branch:** `feat/net-model-v3.1` (live at `/feat/net-model-v3.1/`). **v3.1 is what ships to
  `main`** (design §11) — `dev` stays the playable v3 integration branch.
- **Issues:** epic **#47** · milestone `v3.1`. Fixes **#45**, **#46**; absorbs **#43**, **#42**,
  **#38**; in scope **#37**, **#49**. Closing behavioural re-review: #40, #31, #41, #33, #34, #36.
- **References:** `planning/agent-principles.md` (the constitution), the design doc above,
  `CONTRIBUTING.md`, `GLOSSARY.md`, `planning/2026-07-23-networked-session-model-design.md` (the #35
  model this refines), `docs/diagrams/`.

## Execution

> Sequential TDD, one subagent per task, **HALT on a `null` task**. Pure-logic-first; `src/core`
> stays pure. Commit per task, **never push from a build task**; every commit references **#47** (and
> the specific issue it lands). After each stage run `.claude/workflows/pente-review-gate.mjs` with
> the scope/mutateScope in §Review-gate.

Complexity is given in **tokens** (`words × 1.5`), never time. Totals are per-task implementation +
tests, excluding review-gate rounds.

---

## V.0 — The repro, first · **DONE** (`0aada30`, `e425994`)

The bug that froze v3 now has a deterministic failing test **before** any net code moves, as the
handoff required.

- `cli/netlink.ts` — the mqtt.js connect seam plus a controllable link: `pente drop` kills the socket
  (`reconnectPeriod = 0` + destroy the stream, so the broker's Last-Will fires) under a session that
  keeps its engine/seat/game and still reports `phase: connected`; `pente restore` reconnects. That
  session-vs-transport split is where #45 lives. `--json` on every state verb feeds scripts.
- `cli/scenarios/harness.ts` — each peer is a real `pente play` daemon in its own process with its own
  state dir, identity and DB; two clients that meet only at the broker. Assertions read the daemon's
  own snapshot (proof-by-state).
- `npm run scenario:issue45` — the report replayed verbatim. **Identical on two runs:**

```
✗ phone CAUGHT UP to the live game (ply 2) — ply=1 (laptop=2)
✗ phone's board holds the move made while it was away — pieces[1,1,1]=undefined
✗ phone knows it is ITS turn again — canPlace=false turn=black
✗ the game is not deadlocked — phone.canPlace=false laptop.canPlace=false
✗ phone can still play after reconnecting — move REFUSED
```

Both peers online, both seeing each other present, each waiting on the other, white unable to ever
move: the "bricked" game, mechanised. **This scenario going green is the acceptance test for V.3.**

### What the repro also established (facts the plan depends on)

1. **The presence edge exists on the real relay.** The resident observed the returning peer go
   `absent` (Last-Will) and then `present` again — so "republish on presence" has a real hook.
2. **The suppression risk is real too.** `MqttTransport.acked` dedupes the live-presence ack per peer
   and `PresenceTracker` only notifies when the live SET changes. Absence is what resets both
   (`acked.delete(id)` on an empty body). A blip the broker never turns into an absence therefore
   yields **no** presence transition — so the republish trigger must not depend on an absence having
   been observed (see V.3 guardrail).
3. **Nothing re-runs admission on an mqtt.js auto-reconnect.** The transport re-subscribes and
   re-announces presence; `NetSession` never re-`enter`s, so no `hello` → no `admit` → no state. The
   missed move was published non-retained and is simply gone.

---

## Net-layer map (current, from investigation)

- **`net-room:{code}`** (`session.ts` `roomStateId`/`loadRoomState`/`persistRoomState`, archive marker
  `NET_ROOM_RESULT`) is the root to delete: a game + seat map persisted per room **code**, written on
  establish/admit/arbitrate/rematch and re-loaded by `buildProvisionalSeat` for a `defer` entry.
- **Sync policy** is `decideSync` (`sync.ts:527`) — today it adopts **any** strict extension, however
  many moves ahead. V.3.1's narrow one-move rule replaces exactly this. `decideSyncEpoched` layers
  the rematch generation on top and stays.
- **`SyncEngine.publishState()`** (`sync.ts:981`) already publishes the full log — the primitive
  resident-peer republish needs; no new message kind.
- **Admission** (`admission.ts` `reconcile`/`electInitiator`, message codec + `AdmissionDeduper` in
  `sync.ts`, driven by `NetSession.enter`) is the right shape; the seed matrix is what changes.
- **Seats** are identity-owned on the game (`seats.ts`, `PersistedSeats`) — keep, do not touch.
- **`recentCodes.ts`** (`pente:recentCodes`, injected `Storage`) is already the design's
  `visitedRoomCodes`; reuse it rather than adding a second store.
- **`main.ts`** restores `AUTOSAVE_ID_KEY` on boot — that is the auto-resume §6 forbids.
- **`archive.ts`** has `loadNetGameByUuid` + `listArchivedGames` (which filters the room-state
  records out) — the UUID-keyed base for #37; the filter dies with the marker.

## Guardrails (landmines — each one is a bug we already paid for)

- **Never re-introduce a code→game mapping.** Not in localStorage, not in the archive, and *not as a
  retained MQTT message* — retained state would re-couple code↔game at the broker and kill the
  "re-use DUDEEE forever" model the whole design exists to serve (§4).
- **The breadcrumb is session state, not a mapping.** `activeNetworkedGame` drives a **prompt, never
  an auto-load**, is cleared on completion, and is **never published** (§2).
- **Auto-adopt is ONLY the one-move fast-forward.** Any longer prefix or any fork goes to LCA + diff +
  handshake. The turn gate caps *legitimate* drift at one move, so anything more is already anomalous.
- **Validate an adopted log by REPLAYING it** through the pure rules engine; never trust a sender's
  derived state. Reject on the first illegal entry, honestly.
- **A dumb relay cannot referee** (one shared credential; retained messages are client-written). The
  hash chain + replay-validation + "the opponent's client is the validator" is the threat model. An
  authoritative server is #50, not this build.
- **Rejoin DISPLAYS a derived colour; it never negotiates one** — that is what keeps #31 and #40 shut.
- Admission stays **non-retained + id-deduped**; a reconnect must never replay a stale proposal.
- `randomId.ts`, never `crypto.randomUUID` — the latter is `undefined` over plain-LAN http and
  Playwright (always a secure context) will not catch it.
- Cross-component wiring gets its own test. #35 passed every component gate at 98% mutation while the
  durable seat map was silently not wired.

---

## Build steps

Test tier: **pure** = Vitest unit + fast-check + Stryker mutation (100% coverage pin); **glue** =
Playwright on the real app (`window.__pente` state, two-context relay round-trips) + CLI scenarios —
never log lines.

| # | Step | Key files | Tier | ~tokens |
|---|---|---|---|---|
| **V.1** | **Delete the code↔game coupling.** Remove `net-room:{code}` end to end: `roomStateId`/`loadRoomState`/`persistRoomState`, the `NET_ROOM_RESULT` marker + the listing filter, and `buildProvisionalSeat`'s "defer adopts the room's persisted game" branch. New pure `src/net/activeGame.ts` — the `activeNetworkedGame {code,gameUuid,updatedAt}` breadcrumb over an injected `Storage` (read/write/clear + `updatedAt` staleness), shaped like `recentCodes.ts`. Keep `recentCodes` as `visitedRoomCodes`. A `defer` entry now brings a fresh empty game; the **breadcrumb's uuid** (not the code) is what a returning peer re-seeds from. | create `src/net/activeGame.ts`; `src/net/session.ts`, `src/persist/archive.ts`, `src/ui/widgets/recentCodes.ts`, `GLOSSARY.md` | pure + glue | 14k |
| **V.2** | **Wire-enforced seed semantics (#46, #43, #42).** `reconcile` becomes the §3 matrix: `new` sends/accepts **empty only** and rejects non-empty; `resume`/`current` accept the **same uuid only**; **`defer` is the only kind that adopts a peer's non-empty game**. Mismatch → a typed reject surfaced verbatim (new reason(s), e.g. `seed-refused`). Both-`new` → ONE shared uuid by initiator election (the real #42 fix — shared at genesis, not "converges on the first move"). | `src/net/admission.ts`, `src/net/sync.ts` (reject reasons + codec), `src/net/session.ts`, `src/ui/widgets/netPanelModel.ts` | pure (+ glue) | 16k |
| **V.3** | **Resident-peer republish on presence (#45).** Pure `shouldRepublish(...)` decision (who republishes, on what transition, rate/id-limited, idempotent); glue in `NetSession.onPresence` calling `SyncEngine.publishState()`. **Both directions:** the resident republishes so a returner catches up, and the returner republishes so a resident can fast-forward to a move that never got out (§5 mirror). Trigger on **any fresh live presence from a peer**, not only an `absent→present` edge — per finding (2) an absence may never be observed. Empty room → load own game by uuid from the breadcrumb and wait. | create `src/net/republish.ts` (pure); `src/net/session.ts`, `src/net/mqttTransport.ts` (surface a fresh-live-presence signal), `src/net/presence.ts` | pure + glue | 18k |
| **V.4** | **Reconciliation: narrow auto-FF, else LCA + diff + handshake (#38).** Replace `decideSync`'s blanket adopt with the §5 policy: identical → play on; **exactly one move longer, with my log as its prefix, and my log says it is their turn → fast-forward**; I am one ahead → republish; **anything else → `needs-resolution`** carrying the last common ancestor (walk both hash chains back — free by construction) and a pretty-printed diff. Resolution is chosen by **handshake**, reusing the N.1 out-of-band primitive with resolve actions (take-mine / take-theirs / rewind-to-LCA); nothing lands until both agree. **Replay-validate** any adopted log through `placePiece`. | `src/net/sync.ts` (`decideSync`), create `src/net/reconcile.ts` + `src/net/logDiff.ts` (pure), `src/net/handshake.ts`, `src/net/session.ts`, `src/ui/widgets/` (a divergence panel) | pure + glue | 30k |
| **V.5** | **Reload → empty slate, then a rejoin PROBE (§6).** Boot stops auto-restoring the autosave game. If the breadcrumb exists, probe the room (connect, presence, compare the waiting peer's game uuid from its `hello`) and offer, per the §6 table: same uuid → "Rejoin DUDEEE as **Black**?" (colour **derived** from the game's seat map); different uuid → warn, do not hijack; empty room → "no one is there anymore — rejoin anyway?". Declining clears the breadcrumb; a stale `updatedAt` expires quietly. Pure `deriveRejoinPrompt(probe)`, DOM glue. | `src/main.ts`, `src/net/session.ts`, `src/net/activeGame.ts`, create `src/ui/widgets/rejoinPromptModel.ts` + its widget | pure + glue | 20k |
| **V.6** | **#37 games list / resume UI.** With reload → empty slate and no code→game map, the games list is the ONLY route back to a game — a gap this design creates, so it is in scope. Extend the existing archive widget (browse finished **and** unfinished, resume by UUID, seed the Resume selector) rather than starting fresh. | `src/ui/widgets/archiveModel.ts`, `src/ui/widgets/archive.ts`, `src/persist/archive.ts`, `src/ui/widgets/netPanelModel.ts` | model pure + glue | 18k |
| **V.7** | **The scenario matrix — CLI + two-context e2e.** `scenario:issue45` must go **green** (it already fails for the right reason). Add: code-reuse New-Game (#46), rematch + reconnect (#40 behaviour), both-absent return, and the **FF-vs-diff boundary** (1 move behind = automatic, 2 = resolution handshake). Playwright: empty-slate reload, the three probe prompts, seed-mismatch reject copy. Prove each gate **bites**. | `cli/scenarios/*`, `e2e/sessionModel.spec.ts`, `e2e/networked.spec.ts`, `src/debug/window.ts` | glue | 22k |
| **V.8** | **Close-out.** Behavioural re-review of the v3 tickets per design §10 (#40, #31, #41, #33, #34, #36 — "is this behaviour still required, and does it still hold?"), #49 colour preference, `GLOSSARY.md` terms (breadcrumb, LCA, republish, seed kind), `npm run diagrams`, and the promotion of v3.1 → `test` → `main` (which closes the ~16 v3 tickets with it). | `planning/`, `GLOSSARY.md`, `docs/diagrams/`, issues | docs | 10k |

**Sequencing:** V.1 → V.2 → **V.3** → V.4 → V.5 → V.6 → V.7 → V.8.

V.3 is the early proof point: once it lands, `npm run scenario:issue45` should flip to green with
V.4's policy still unwritten — a real, checkable milestone a third of the way in. V.4 is the largest
piece and depends on V.3 only for the transport-level "when do we exchange state" question.

## Decisions taken mid-build

### One record per game — key every autosave by `game.uuid`, retire `autosaveId`

- **Status:** Accepted (2026-07-25). Assigned to **V.5**, which already rewrites the boot/restore path
  that owns `AUTOSAVE_ID_KEY`. V.6 must inherit one record per game, not a listing heuristic.
- **Context:** V.1 deleted `persistRoomState`, the only writer of the identity-owned seat map, so the
  session began archiving the authoritative game under its **own uuid** (design §2 "games keyed by
  UUID"). That collided with `main.ts`'s autosave writing the same game under the app's `autosaveId`
  → two archive records for one networked game. Two e2e tests caught it. V.1's fix made the two
  writers *cooperate*: a shared `playersFromSeats` projection, `archivedStartedAts` priming so both
  agree on the date, and a **shadow-collapse rule** in `listArchivedGames` that hides the
  non-canonical copy. It works, and every piece of it exists only because one game has two records.
- **Rationale:** since #35/S.1 **every `Game` already carries a uuid**, so keying every autosave
  record by `game.uuid` — local games included — makes one game have exactly one record by
  construction, and the cooperation machinery deletes. The reviewer-found `autosaveMeta` bug (one
  metadata object gated on two different predicates, so a post-conflict local record took the
  conflicted net game's date) was a symptom of the same double-write. Expect a net removal of code.
  User, asked whether to keep the arrangement or remove it: *"yeah, no, remoing that machinery seems
  good :)"*
- **Constraint:** the re-keying must be a migration, not a loss — records already stored under an old
  autosave id carry `meta.uuid` and must stay listed and resumable.

### Codes longer than `CODE_LENGTH` are accepted — left to #30

- **Status:** Deferred to **#30** (code-validation fixes), pinned meanwhile by a characterization test
  in `src/net/activeGame.test.ts`.
- **Context:** found while fixing a hollow fast-check property in V.1's review: `validateGameCode`
  refuses a code SHORTER than `CODE_LENGTH` but has no upper bound, so a 7-character code validates
  and a breadcrumb round-trips it. Pre-existing on `dev`.
- **Rationale:** the breadcrumb reader defers to the shared validator by design (one code rule, not
  two), so tightening it changes what the **join** path accepts — a user-visible rule change that
  belongs with #30 rather than inside a v3.1 breadcrumb commit.

## Review-gate (`pente-review-gate.mjs` args)

- `stage`: `"net-model-v3.1"`
- `scope`: `"src/net src/persist src/ui/widgets src/core src/debug cli"`
- `mutateScope` (**pure only**): `"src/net/admission.ts src/net/sync.ts src/net/reconcile.ts src/net/logDiff.ts src/net/republish.ts src/net/activeGame.ts src/net/seats.ts src/net/handshake.ts src/ui/widgets/netPanelModel.ts src/ui/widgets/archiveModel.ts src/ui/widgets/rejoinPromptModel.ts"`
  — glue (`session.ts`, `appSession.ts`, `mqttTransport.ts`, widgets' DOM halves, `cli/`, e2e) is
  behaviour-verified, never mutation-tested.

## Collaboration points (hands-on, decide live with the user)

- The **divergence panel**: how the diff is rendered and how the three resolutions are worded — this
  is the one place a player meets the protocol, and it must not read like a merge conflict.
- The **rejoin prompt** copy for the three probe outcomes (§6), including the "different game in the
  room" warning that must not sound like an error.
- Whether a `resume`/`current` seed should warn before it pushes a concrete game into a shared room.
- #49 colour preference on new games — preference, not a claim; election on a tie.

## Verification (per gate, independently re-run)

Build (`tsc --noEmit` + vite) 0 · `npm run typecheck:cli` 0 · lint 0 warnings · coverage 100% on pure
files · mutation ≥95 on `mutateScope` · unit + fast-check green · Playwright green (two-context specs
re-run with `--workers=1` before calling any failure real) · **`npm run scenario:issue45` exit 0** plus
the V.7 scenarios · diagrams regenerated before `test`/`main`. Every gate proven to bite; a flaky
metric re-run ≥2×; agent claims re-verified against the committed SHA (`git log`/`git status`), never
taken from a completion notice.
